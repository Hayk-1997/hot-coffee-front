import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(theme => ({
    root: {
        height: '100vh',
    },
    container: {
        height: '100%',
        margin: '0 auto',
        display: 'flex',
        alignItems: 'center',
    },
    loginForm: {
        width: '100%',
    }
}));